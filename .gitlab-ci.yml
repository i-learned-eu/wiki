image: node:latest

stages:
  - test
  - deploy

test:
  stage: test
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - build

pages:
  stage: deploy
  script:
    - mv ./build ../public
  artifacts:
    paths:
      - public
  dependencies:
    - test

  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH